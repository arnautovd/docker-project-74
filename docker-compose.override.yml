services:
  app:
    image: dianaarnautova/docker-project-74
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./:/app
      - app_node_modules:/app/node_modules
    command: make dev
    ports:
      - "8080:8080"

volumes:
  app_node_modules:
